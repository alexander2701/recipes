<%- include('partials/header') -%>

<section class='container-fluid'>

  <h2 class='heading-md'><%= recipe.name %></h2>
  <h3 class="heading-sm">Bereidingstijd <%= recipe.preparationTime %> minuten</h3>
  <h3 class="heading-sm">Ingredienten</h3>
  <ul>
    <% recipe.ingredients.forEach(ingredient => { %>
      <li><%= ingredient %></li>
  <% }) %>
  </ul>

</section>

<%- include('partials/footer') -%>
